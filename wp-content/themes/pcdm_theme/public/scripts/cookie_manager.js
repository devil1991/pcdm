// Generated by CoffeeScript 1.6.3
(function() {
  var __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

  window.CookieManager = (function() {
    function CookieManager() {
      this.setCookie = __bind(this.setCookie, this);
      this.getCookie = __bind(this.getCookie, this);
    }

    CookieManager.prototype.getCookie = function(name) {
      var end, offset, search, value;
      value = '';
      if (document.cookie.length > 0) {
        search = "" + name + "=";
        offset = document.cookie.indexOf(search);
        if (offset !== -1) {
          offset += search.length;
          end = document.cookie.indexOf(';', offset);
          if (end === -1) {
            end = document.cookie.length;
          }
          value = unescape(document.cookie.substring(offset, end));
        }
      }
      return value;
    };

    CookieManager.prototype.setCookie = function(name, value, exp_months, domain) {
      var exp_date;
      exp_date = new Date();
      exp_date.setMonth(exp_date.getMonth() + exp_months);
      return document.cookie = "" + name + "=" + (escape(value)) + ";expires=" + exp_date;
    };

    return CookieManager;

  })();

}).call(this);
