// Generated by CoffeeScript 1.6.3
(function() {
  $(function() {
    var contentCheck, posts_prev,
      _this = this;
    posts_prev = 0;
    contentCheck = function() {
      var aux, hover, i, j, pos, post, posts, posts_length, tag, tags, text, _i, _j, _ref;
      posts = $('section.content .post');
      posts_length = posts.length;
      if (posts_length !== posts_prev) {
        for (i = _i = 0; 0 <= posts_length ? _i < posts_length : _i > posts_length; i = 0 <= posts_length ? ++_i : --_i) {
          post = $(posts[i]);
          if (!post.hasClass('js-checked')) {
            tags = post.find('>.hover .tags >li >a');
            for (j = _j = 0, _ref = tags.length; 0 <= _ref ? _j < _ref : _j > _ref; j = 0 <= _ref ? ++_j : --_j) {
              tag = $(tags[j]);
              text = tag.text().substring(1);
              if (text === 'small' || text === 'medium' || text === 'large') {
                tag.remove();
              }
            }
            hover = post.find('>.hover');
            aux = hover.find('>.wrap-text >.aux-text');
            pos = .5 * (hover.height() - aux.height());
            aux.css({
              top: "" + pos + "px"
            });
            if (!(pos < 0)) {
              post.addClass('js-checked');
            }
          }
        }
        posts_prev = posts_length;
      }
      return setTimeout(contentCheck, 1000);
    };
    return $(window).load(contentCheck);
  });

}).call(this);
